#ifndef UTILS_H
#define UTILS_H

void sort(map<int, long>& M);

struct comp {
    public:
    template <typename T> 
  
    // Comparator function 
    bool operator()(const T& l, 
                    const T& r); const
};
#endif